
  <h4> <%= @post.title %></h4>

  <h6> <%= @post.user.full_name %> | <%= @post.get_formatted_post_time %></h6>

  <div class="contents">
<%= raw @post.content %>
  </div> 

  <div> 
    <%= link_to 'Edit', edit_post_path(@post) if @post.can_edit? session[:user] %>
  </div>
